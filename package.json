{
  "scripts": {
    "install:jekyll": "bundle config set path vendor/bundle && bundle install",
    "serve": "./preview.sh"
  },
  "devDependencies": {
    "@shopify/prettier-plugin-liquid": "^1.10.0",
    "prettier": "^3.8.0"
  }
}
